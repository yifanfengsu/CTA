# 预注册文档模板（LOCKED，看结果前写死并单独 commit）

> 提炼自 `vrp/reports/atm_vrp_stageB_premium_truth_20260628/PREREGISTRATION.md`
> （LOCKED d4597b2 先于结果 commit）与 `research/_closed/crypto_perp/pairs_cointegration/reports/pairs_cointegration_20260613/`
> 的置顶三段声明。使用方法：复制本模板到研究产物目录，命名 `PREREGISTRATION.md`，
> 填完后**先于任何结果单独 commit**，hash 写进最终报告。

---

# <研究线名> 预注册 — <一句话问题>

**LOCKED <YYYY-MM-DD>，看结果前写死（铁律 A）。本文件先于任何结果 commit。**

## 0. 机制登记卡（承接第 1 站）

- **机制依据声明**：为什么这里该有钱？谁在付钱、为什么钱还在？（一段话）
- **先验声明**：对照死因库（`.claude/skills/PIPELINE.md` §4），本想法最可能死于
  哪条？先验（中性/偏负/偏正）+ 理由。
- **样本偏差声明**：幸存者/方向/regime 偏差的方向标注（偏乐观 → 判死更稳、
  判活须打折）。

## 1. 口径（全部写死）

| 项 | 取值 | 理由 |
|---|---|---|
| 数据源 | <显式路径 + server 字段> | 数据环境铁律 |
| 样本窗口 | <起止，选择理由须与结果无关（availability-driven）> | |
| 币种/标的宇宙 | <全集，非事后挑选> | |
| 成本栈 | <费率/滑点/funding，显式> | 报告头部必列 |
| 频率/持有期 | <单一预注册值；若有邻域，声明"仅验稳健不选最优"> | 防参数尖峰 |
| 关键参数的经济理由 | <如 EWMA 半衰期=中位持仓尺度> | 不对样本内挑最优 |

## 2. trial-ledger（初版）

| # | 配置 id | 参数 | 登记时间 | 产物落盘位置（须可还原 Sharpe/日收益） |
|---|---|---|---|---|
| 1 | | | | |

（中途新增配置**逐条追加**，不许事后补记。本表终版交 `multiple-testing/` 作 N。）

## 3. gate（数值阈值 + 结构要求，全部先写死）

- **G1 <名>**：<数值阈值> 且 <结构要求：跨 ≥K 币 / 双样本同向 / 邻域同号 等>
- **G2 <名>**：…
- **gate 0（样本方向中性核对，如适用）**：样本净漂移/单向占比阈值；超阈 → 全部
  结论标注 conditional。
- **PASS ⟺** <逻辑组合>；**FAIL ⟺** <任一触发条款，含"流动性伪装"类综合条款>。
- 不算 Sharpe 主判（铁律 C）／左偏不用 mean 做可行性门（VRP 教训）——按偏态声明
  主判指标。

## 4. 情景集（如适用，左偏/卖保险类必填）

① 历史最坏 × 2；② 反向极端（上行 squeeze）；③ whipsaw/双向抽插。
各情景对 mean 与 bootstrap 下界的响应进判定。

## 5. 裁定规则

- 字面 gate 判定原样报告；综合判定若与字面分歧，两者并列 + 分歧方向必须朝保守。
- 预注册 gate 判死后不得重定义复活；复活仅可经另一个事先写好的不同 gate。
- 验证周期算术（铁律 B）：打折后 Sharpe + bootstrap SE 反推；超资源窗口不立项。

---
*本预注册在生成任何结果数值前 commit。结果写入同目录 README.md。*
