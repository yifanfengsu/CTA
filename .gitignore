.venv/
.env
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.vntrader/
database.db
*.db
*.db-journal
*.sqlite
logs/*
!logs/.gitkeep
data/raw/*
!data/raw/.gitkeep
data/funding/okx/*.csv
data/funding/okx_historical_raw/*
# Binance vision cross-validation zips (344MB; README + manifest stay tracked)
data/binance_vision/**/*.zip
# reports/ is version-controlled since 2026-06-11 (vulnerability patch):
# postmortems, READMEs, json/jsonl/csv results are knowledge assets.
# Only single files >50MB are excluded (listed individually below).
reports/research/trend_capture_exit_convexity/exit_variant_trades.csv

# Basis-arb raw data layer (reproducible via scripts/research_basis_feasibility.py --download;
# manifest.json stays tracked, analysis-ready series live in reports/basis_arbitrage_feasibility_*/)
data/basis/futures/*.csv
data/basis/index/*.csv

# B2_4h forward observation: runtime data store + state + secrets are NOT tracked
# (config_frozen.json, gates_preregistered.md, baseline_distribution.json,
#  dry_run_validation.json, README/manual/REVIEW templates, notify.conf.template ARE tracked)
forward/data/
forward/state/
forward/notify.conf
forward/last_run_log.txt

# MR-5m runtime state and trade logs
mr_5m_state.json
trade_log_5m*.jsonl
*.bak
